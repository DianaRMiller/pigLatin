var pigLatin = function(word) {
  if (["a","e","i","o","u"].includes(word[0])); {
    return (word.concat("ay"));
  };
};

//var pigLatin = function(word) {
//  if (word[0].indexOf([a-zA-Z]-["a","e","i","o","u"]))
//  return (word);
//};
